{
  "name": "powrs-2021",
  "private": "true",
  "author": {
    "name": "Drew Powers",
    "email": "drew@pow.rs"
  },
  "type": "module",
  "scripts": {
    "dev": "npm run build:js && astro dev",
    "build": "npm run build:js && astro build",
    "build:js": "esbuild --bundle --minify public/3d.js --outfile=public/3d.min.js --target=esnext --format=esm"
  },
  "prettier": {
    "printWidth": 240,
    "singleQuote": true,
    "trailingComma": "all"
  },
  "dependencies": {
    "three": "^0.154.0"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^2.0.0",
    "astro": "^2.8.5",
    "esbuild": "^0.18.15",
    "prettier": "^2.8.8",
    "sass": "^1.64.0",
    "shiki": "^0.14.3"
  }
}
