<style lang="scss">
  .loading-container {
    position: relative;
    height: 8rem;
  }

  .tick {
    animation: highlight 4s infinite, flicker 9s infinite, flash 5s infinite;
    background-color: rgba(0, 0, 96, 0.2);
    border-radius: 1em;
    height: 15px;
    left: 50%;
    margin-left: -3px;
    opacity: 0;
    position: absolute;
    transform-origin: 50% 200%;
    top: 0%;
    width: 6px;

    &:nth-child(2) {
      animation: highlight 4s 0.5s infinite, flicker 23s infinite, flash 3s 3s infinite;
      transform: rotate(45deg);
    }
    &:nth-child(3) {
      animation: highlight 4s 1s infinite, flicker 3s 5s infinite;
      transform: rotate(90deg);
    }
    &:nth-child(4) {
      animation: highlight 4s 1.5s infinite, flicker 4s 2s infinite, flash 7s infinite;
      opacity: 0;
      transform: rotate(135deg);
    }
    &:nth-child(5) {
      animation: highlight 4s 2s infinite, flicker 7s infinite;
      transform: rotate(180deg);
    }
    &:nth-child(6) {
      animation: highlight 4s 2.5s infinite, flicker 19s infinite, flash 5s 7s infinite;
      opacity: 0;
      transform: rotate(225deg);
    }
    &:nth-child(7) {
      animation: highlight 4s 3s infinite, flicker 5s infinite;
      opacity: 0;
      transform: rotate(270deg);
    }
    &:nth-child(8) {
      animation: highlight 4s 3.5s infinite, flicker 9s 3s infinite, flash 3s 4s infinite;
      transform: rotate(315deg);
    }
  }

  @keyframes highlight {
    0%, 12.4% {
      background-color: rgba(0, 0, 96, 0.6);
    }
    12.5%, 100% {
      background-color: rgba(0, 0, 96, 0.2);
    }
  }

  @keyframes flicker {
    0%, 89.9% {
      opacity: 1;
    }
    90%, 100% {
      opacity: 0;
    }
  }

  @keyframes flash {
    0%, 44.9%, 55%, 100% {
      filter: none;
    }
    45%, 54.9% {
      filter: saturate(5) hue-rotate(90deg);
    }
  }
</style>

<div class="loading-container">
  <div class="tick"></div>
  <div class="tick"></div>
  <div class="tick"></div>
  <div class="tick"></div>
  <div class="tick"></div>
  <div class="tick"></div>
  <div class="tick"></div>
  <div class="tick"></div>
</div>

<slot />
