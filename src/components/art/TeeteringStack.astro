<style lang="scss" is:global>
  .stack-wrapper {
    position: relative;
    height: 6rem;
    margin-bottom: 1.5rem;

    > .layer {
      background-color: orangered;
      > .layer {
        background-color: rebeccapurple;
        > .layer {
          background-color: coral;
          > .layer {
            background-color: turquoise;
            > .layer {
              background-color: blueviolet;
              > .layer {
                background-color: tomato;
                > .layer {
                  background-color: royalblue;
                  > .layer {
                    background-color: orange;
                  }
                }
              }
            }
          }
        }
      }
    }
  }

  .layer {
    animation: sway 8s cubic-bezier(0.5, 0, 0.5, 1) infinite;
    background-color: orange;
    bottom: 0;
    height: 9px;
    left: 50%;
    margin-left: -1.5rem;
    position: absolute;
    transform: translateX(-0.25rem) translateY(calc(-100% - 4px)) rotate(-6deg);
    transform-origin: 50% 100%;
    width: 48px;
  }

  @keyframes sway {
    50% {
      transform: translateX(0.25rem) translateY(calc(-100% - 4px)) rotate(6deg);
    }
  }
</style>

<div class="stack-wrapper">
  <div class="layer">
    <div class="layer">
      <div class="layer">
        <div class="layer">
          <div class="layer">
            <div class="layer">
              <div class="layer">
                <div class="layer"></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
